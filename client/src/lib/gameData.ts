// Resetting game data for LIFE.EXE revert
export type Player = {
    id: number;
    name: string;
    // ... minimal types just to satisfy any lingering imports if needed
};

export const INITIAL_PLAYERS = [];
export const EVENTS = [];
